<div>
  <h3>Edit <%= @category.title %></h3>
  <form method="post" action="/category/<%= @id %>/edit">
    <label>Category Title: </label>
    <input name="cat_title" type="text" value="<%= @category.title %>">
    <label>Allotted Monthly Funds for Category: </label>
    <input name="cat_allotted_funds" type="number" value="<%= @category.amount %>">
    <button type="submit">Update Category</button>
  </form>
</div>

<%= @category.id %>
<%= @category.title %>
<%= @category.amount %>
